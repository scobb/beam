# Ralph Progress Log
Started: Wed Apr 15 03:01:00 PM PDT 2026
---

## 2026-04-15 - BEAM-237
- Implemented GET /pricing page with Free ($0) and Pro ($5/mo) plan comparison
- Added feature comparison table covering 11 features across both plans
- Added FAQ section: free trial, pageview limits, cancellation, credit card, comparisons, refunds
- Added CTAs: "Start free" → /signup, "Get Pro" → /signup, bottom section with demo link
- Added /pricing to sitemap.xml with priority 0.9 (high SEO page)
- Files changed: src/routes/pricing.ts (new), src/index.ts (import + route + sitemap)
- Added 4 smoke tests (8 total across desktop+mobile): 200 response, content, sitemap, mobile overflow
- Smoke tests pass locally and on prod (beam-privacy.com)
- **Learnings for future iterations:**
  - Pricing numbers in the live site: Free = 50K pageviews/mo, Pro = $5/mo 500K — the PRD story had stale numbers (5K/100K); always check the landing page for authoritative values
  - New route pattern: create src/routes/foo.ts, import in src/index.ts, add `app.route('/', foo)`, add to sitemap paths + meta objects
  - Route registration error on deploy is pre-existing (auth token lacks zone:edit); worker upload still succeeds and routes work in prod
---

## 2026-04-15 - BEAM-238
- Added /for/nuxt Nuxt 3 integration guide
- Guide uses Nuxt plugin system (plugins/beam.client.ts) with .client.ts SSR exclusion
- Two install options: script-tag client plugin and npm package
- Added nuxt to GUIDE_SECTIONS 'Developer frameworks' list (between vue and wordpress)
- Added /for/nuxt to sitemap.xml
- Added /for/vue and /for/nuxt links to landing page step 1 inline list
- Vue's related guides updated to include nuxt
- Files: src/routes/for.ts, src/index.ts, src/landing.ts, test/smoke/smoke.spec.ts
- 6 smoke tests (12 desktop+mobile), all pass locally and on prod
- **Learnings for future iterations:**
  - Adding a /for/* guide requires: (1) add to GUIDES object, (2) add slug to GUIDE_SECTIONS.slugs, (3) add to sitemap paths+meta, (4) optionally link from landing.ts step 1 list
  - GUIDE_SECTIONS uses `as const` — TypeScript will error if a slug doesn't exist in GUIDES
---

## 2026-04-15 - BEAM-239
- Added /for/svelte: onMount-based script injection (SSR-safe), index.html alternative for Vite+Svelte
- Added /for/sveltekit: +layout.svelte with afterNavigate for SPA tracking, app.html alternative
- Added svelte and sveltekit to GUIDE_SECTIONS developer frameworks list (between nuxt and wordpress)
- Added /for/svelte and /for/sveltekit to sitemap.xml
- Files: src/routes/for.ts, src/index.ts, test/smoke/smoke.spec.ts
- 8 smoke tests (16 desktop+mobile), all pass on prod after brief propagation delay
- **Learnings for future iterations:**
  - Cloudflare Workers deployment can have ~10-30s propagation delay — if prod smoke tests fail immediately after deploy, wait and retry before blocking the story
  - GUIDE_SECTIONS `as const` restriction: both slugs must exist in GUIDES before adding to slugs array
---

## 2026-04-15 - BEAM-240
- Added og:image, twitter:card, twitter:title, twitter:description to all 9 blog post pages (replace_all pattern on shared template literal)
- Added og:image + twitter tags to blog index, /about, /pricing, /changelog, /privacy, /terms
- Upgraded landing page twitter:card from 'summary' to 'summary_large_image'
- og:image uses /og-image.svg (already served at that path — no new asset needed)
- Blog posts use twitter:card=summary_large_image; other pages use summary
- Files: src/routes/blog.ts, about.ts, pricing.ts, changelog.ts, legal.ts, src/landing.ts
- 6 smoke tests (12 desktop+mobile), all pass on prod
- **Learnings for future iterations:**
  - blog.ts has 9 posts each with identical template literal patterns — `replace_all: true` handles all at once without touching each handler individually
  - /og-image.svg is already served and suitable for og:image across all pages; no need to create /og-image.png
  - vs.ts, migrate.ts, for.ts, howItWorks.ts, tools.ts, wordpressPlugin.ts were already complete; only blog/about/pricing/changelog/legal/landing needed fixes
---

## 2026-04-15 - BEAM-241
- Added fire-and-forget security email to POST /dashboard/settings/password on successful password change
- Email from: 'Beam Security <ralph@keylightdigital.dev>', subject: 'Your Beam password was changed'
- Body includes account email, UTC timestamp, and support contact instructions
- Email failure does not block the redirect (Promise.catch swallows errors, no await)
- Only sends if RESEND_API_KEY is present in env
- Files: src/routes/dashboard.ts
- Smoke tests: 2/2 pass locally (desktop + mobile)
- PROD NOTE: prod smoke tests could not run due to rate limit exhaustion (10 signups/hr/IP, 429 returned)
  This is not a code failure — feature is correct, rate limit is a known infra constraint (documented in ralph-context.md)
- UNVERIFIED: security email delivery not testable in smoke suite (fire-and-forget)
- **Learnings for future iterations:**
  - Scope form locators by action attribute to avoid clicking the wrong submit button: `page.locator('form[action="/path"]').locator('button[type="submit"]')`
  - `fetch()` with `redirect: 'manual'` in browser context returns type=opaqueredirect with status 0, not the actual status code — use page navigation instead
  - prod signup rate limit (10/hr/IP) gets exhausted across a session with many smoke runs; space out prod test runs between stories
---

# Ralph Progress Log
Started: Mon Apr 13 06:34:00 PM PDT 2026
---

## Codebase Patterns
- Email sending in collect.ts uses `c.executionCtx.waitUntil()` for fire-and-forget async ops
- KV dedup keys follow pattern `{prefix}:{userId}:{YYYY-MM}` for per-user-per-month guards
- Resend API sends from `beam@keylightdigital.dev`, timeout 4000ms, errors are swallowed
- Monthly count is captured pre-insert as `capturedMonthlyCount`; add +1 for the record just inserted
---

## 2026-04-14 - BEAM-217
- Added `src/lib/limitWarning.ts`: sends one warning email per free user per calendar month when their monthly pageview count reaches 40K (80% of 50K limit)
- KV key `limitWarn:{userId}:{YYYY-MM}` ensures exactly one email per user per month
- Modified `src/routes/collect.ts`: hoisted `capturedMonthlyCount` to outer scope, added `waitUntil(maybeSendLimitWarning(...))` in pageview branch for free users
- Added smoke test in `test/smoke/smoke.spec.ts`: verifies collect returns 204 (async warning does not block response)
- Files changed: `src/lib/limitWarning.ts` (new), `src/routes/collect.ts`, `test/smoke/smoke.spec.ts`
- Deployed to prod via `deploy.sh prod` — worker upload succeeded, route registration fails on CF zone auth (pre-existing issue, routes already exist)
- Prod smoke tests: 2/2 pass
- **Learnings for future iterations:**
  - `capturedMonthlyCount` is pre-insert count; pass +1 to warning so threshold check is accurate
  - KV.put before fetch-email prevents double-send even if two isolates race; email errors are swallowed (KV key stays set, no retry)
  - deploy.sh prod: worker upload succeeds even when route registration errors (CF zone auth issue)
---

## 2026-04-14 - BEAM-218
- Added Share button to `/public/:site_id` for logged-in site owners: dropdown with "Copy link" (clipboard API + visual feedback) and "Share on X" (pre-filled tweet with @BeamPrivacy)
- Owner detection: reads `beam_session` JWT cookie, verifies JWT, queries DB for `sites WHERE id = ? AND user_id = ?` — no redirect on failure
- Updated footer: "Analytics by Beam — Privacy-first, cookie-free" with beam-privacy.com links; visible to all visitors
- Added smoke test: creates public site as owner, verifies Share button visible, footer text correct, no horizontal overflow at 375px
- Files changed: `src/routes/public.ts`, `test/smoke/smoke.spec.ts`
- Deployed to prod via `deploy.sh prod` — worker upload succeeded (route registration pre-existing CF auth error)
- Prod smoke tests: 2/2 pass on beam-privacy.com
- **Learnings for future iterations:**
  - Owner detection on public pages: use `getCookie(c, 'beam_session')` + `verifyJWT` + DB ownership check — no auth middleware needed
  - `navigator.clipboard.writeText()` only works on HTTPS; fine for prod but local wrangler dev needs a secure origin
  - PRD userStories key is `userStories`, not `stories` — use `prd['userStories']` when scripting
---

## 2026-04-14 - BEAM-220
- Added site health indicators to the sites overview page (/dashboard/sites)
- Modified `/dashboard/sites` route to LEFT JOIN pageviews for recent data (7-day window)
- Added a second query for sites with no recent data to check if any historical data exists
- Health status: green = data in last 7d, yellow = historical but none in 7d, red = no data ever
- `renderHealthIndicator()`: renders a colored dot (green/yellow/gray) with hover+focus tooltip; red dots link to install verification page
- `computeSiteHealth()`: pure function mapping (lastPageviewAt, now) → SiteHealthStatus
- Mobile-safe: dot is inline-flex, no overflow at 375px
- Added 2 smoke tests: red indicator for new site (no data), mobile overflow at 375px
- Files changed: `src/routes/dashboard.ts`, `test/smoke/smoke.spec.ts`
- Also blocked BEAM-219 (external directory submissions require manual auth), sent email to Steve
- Deployed to prod via `deploy.sh prod` — worker upload succeeded (pre-existing CF route auth error)
- Prod smoke tests: 4/4 new BEAM-220 tests pass, 180/180 full suite passes locally
- **Learnings for future iterations:**
  - Left JOIN with a timestamp filter is clean for "recent activity" queries; follow up with a second query only for the subset that needs historical check (avoids scanning all pageviews for active sites)
  - `data-testid` attributes on the health indicator wrapper make it easy to target in tests even when the dot changes color
  - Tooltip opacity via `group-hover:opacity-100 group-focus-within:opacity-100` works on both desktop hover and keyboard/tap focus
  - `title` attribute on the tooltip wrapper provides accessible fallback
---

## 2026-04-14 - Self-generation (post-BEAM-220)
- Audited beam-privacy.com with Playwright at desktop (1280px) and mobile (375px)
- Checked landing, /for index, /changelog, /blog, /public/dfa32f6b... (public dashboard), /about, /vs/google-analytics
- **Findings:**
  - Changelog last updated April 3 — BEAM-217, 218, 220 all missing (stale credibility issue)
  - "Blog" not linked in main nav — only in footer (discoverability gap)
  - No /for/react guide — React+Vite is the #1 JS stack, high SEO value
  - No /vs/mixpanel page — Mixpanel is a major competitor with high search volume
  - Free users near the 50K limit have no in-dashboard nudge until they get the email at 80%
  - Public dashboard share links have no UTM attribution
  - Sites overview has no per-site monthly usage visibility (billing page only)
- Generated 8 new stories: BEAM-221 through BEAM-228
  - BEAM-221 (high): Update changelog with April 14 entries
  - BEAM-222 (medium): Add Blog to main nav
  - BEAM-223 (medium): /for/react guide
  - BEAM-224 (medium): /vs/mixpanel comparison page
  - BEAM-225 (medium): Upgrade nudge banner for free users at 60%+ usage
  - BEAM-226 (low): UTM tags on share button links
  - BEAM-227 (low): Blog post for April 2026 features
  - BEAM-228 (medium): Per-site monthly usage on sites overview
---

## 2026-04-14 - BEAM-221
- Added April 14, 2026 changelog group at the top of CHANGELOG array in `src/routes/changelog.ts`
- Three entries added:
  - "Site health indicators on the sites overview" (tag: New) — BEAM-220
  - "Share button and Beam footer on public dashboards" (tag: New) — BEAM-218
  - "Usage warning email at 80% of free plan limit" (tag: Improvement) — BEAM-217
- Files changed: `src/routes/changelog.ts`, `prd.json`
- Deployed to prod via `deploy.sh prod` — worker upload succeeded, route registration fails (pre-existing)
- Verified: `GET https://beam-privacy.com/changelog` returns 200 and shows all three new April 14 entries
- **Learnings for future iterations:**
  - Changelog is fully hardcoded in `src/routes/changelog.ts` as a `CHANGELOG: ChangelogGroup[]` array — no DB
  - New date groups go at the TOP of the array (most recent first)
---

## 2026-04-14 - BEAM-222
- Added Blog link to main landing page navigation (`src/landing.ts`) between Changelog and Log in
- Other nav-heavy pages (changelog.ts, blog.ts) already had Blog link; landing.ts was the only gap
- Added 2 smoke tests: desktop (Blog link visible) + mobile 375px (Blog link visible, no overflow)
- Files changed: `src/landing.ts`, `test/smoke/smoke.spec.ts`
- Deployed to prod via `deploy.sh prod` — worker upload succeeded (pre-existing CF route auth error)
- Prod smoke tests: 4/4 pass on beam-privacy.com
- **Learnings for future iterations:**
  - Each page/route has its own `nav()` function — no shared nav component; check all nav functions when adding global links
  - First prod smoke run failed (screenshot showed old nav) — likely CDN propagation; second run 30s later passed
---

## 2026-04-14 - BEAM-225
- Updated `/dashboard` route to show upgrade nudge banners for free users:
  - 60-79%: yellow `bg-yellow-50/border-yellow-200` banner (informational)
  - 80%+: orange `bg-orange-50/border-orange-300` banner (urgent, mentions data-pause)
  - Added `data-testid="upgrade-nudge-warn"` and `data-testid="upgrade-nudge-urgent"` for targeting
  - Pro users and users under 60% see no banner
- Removed old 90%-only banner (was too conservative)
- Added 2 smoke tests: fresh free user sees no banner; dashboard is mobile-safe at 375px
- Files changed: `src/routes/dashboard.ts`, `test/smoke/smoke.spec.ts`
- Deployed to prod via `deploy.sh prod` — worker upload succeeded (pre-existing CF route auth error)
- Prod smoke test note: first desktop run passed 2/2; subsequent runs hit signup rate limit (10/hour/IP) from cumulative sessions — not a code issue
- **Learnings for future iterations:**
  - Upgrade nudge banner already had a 90%+ threshold; replaced with tiered 60/80% logic
  - `data-testid` on banner containers makes Playwright assertions clean (especially for "not visible" assertions)
  - Prod signup rate limit (10/hour/IP) gets exhausted when running multiple full smoke runs in one session; spread test runs across hours
---

## 2026-04-14 - BEAM-228
- Added per-site monthly pageview usage to `/dashboard/sites` site cards
- One efficient GROUP BY query for all sites (not N+1): `SELECT site_id, COUNT(*) as count FROM pageviews WHERE site_id IN (...) AND timestamp >= ? AND timestamp < ? GROUP BY site_id`
- `renderUsage()` function: free users see "X / 50K" + thin color-coded progress bar (indigo <60%, yellow 60-79%, orange 80%+); pro users see "X pageviews this month" (no limit)
- Usage widget positioned left of Analytics/Goals/Manage links on desktop; wraps naturally on mobile
- `data-testid="site-usage-{siteId}"` for Playwright targeting
- Also queries plan from DB (same pattern as /dashboard) to know free vs pro
- Files changed: `src/routes/dashboard.ts`, `test/smoke/smoke.spec.ts`
- Deployed to prod via `deploy.sh prod` — worker upload succeeded (pre-existing CF route auth error)
- Prod smoke tests: all 429 from IP signup rate limit (cumulative session issue, not code); local 4/4 pass
- **Learnings for future iterations:**
  - `renderUsage()` is a local helper function within the route handler — matches the pattern of `renderHealthIndicator()`
  - Progress bar colors match BEAM-225 nudge banner thresholds (60%/80%) for visual consistency
  - Prod signup rate limit (10/hour/IP) consistently exhausted when running 3+ smoke stories in one hour
---

## 2026-04-14 - BEAM-223
- Added `/for/react` integration guide for React + Vite SPAs
- Two install steps: (1) script tag in `public/index.html`, (2) optional `useBeam` hook with `useEffect` + `useLocation` for SPA route tracking
- Added `react` to `GUIDE_SECTIONS` developer frameworks array → hub page auto-renders guide card
- Added `{ slug: 'react', name: 'React' }` to Next.js guide's `others` list and guide page footer links
- Added `/for/react` to sitemap paths and meta in `src/index.ts`
- 6 smoke tests pass on prod: content check, hub+sitemap, mobile responsiveness
- Files changed: `src/routes/for.ts`, `src/index.ts`, `test/smoke/smoke.spec.ts`
- First prod run: 5/6 pass (sitemap test flaked — CDN cache race); second run 6/6 pass
- **Learnings for future iterations:**
  - Adding a guide requires: GUIDES entry, GUIDE_SECTIONS slug, sitemap paths+meta, footer links
  - Hub page auto-renders from GUIDE_SECTIONS — no separate template change needed
  - CDN propagation can cause flaky sitemap tests on first prod run after deploy; retry resolves it
---

## 2026-04-14 - BEAM-224
- Added `/vs/mixpanel` comparison page (cookie-free vs Mixpanel cookies, $5/mo flat vs per-event pricing, 5-min setup vs hours of instrumentation)
- Structured: comparison table, prose analysis (where each wins), FAQ JSON-LD, related comparisons (PostHog, Plausible, Google Analytics)
- Added to sitemap.xml and footer link list in vs.ts
- Also fixed two pre-existing mobile issues while implementing:
  - `comparisonTable()` now wraps table in `<div class="overflow-x-auto">` + `min-width:480px` for mobile scrollability
  - Footer link container: `flex items-center gap-6` → `flex flex-wrap items-center gap-x-6 gap-y-2` so 20+ links wrap on mobile
- 6 smoke tests pass on prod: content, sitemap, mobile 375px (both desktop and mobile Playwright projects)
- Files changed: `src/routes/vs.ts`, `src/index.ts`, `test/smoke/smoke.spec.ts`
- **Learnings for future iterations:**
  - `assertNoHorizontalOverflow` at true 375px (desktop Chrome) is stricter than the Mobile project at 393px — catches more overflow issues
  - The vs.ts footer inner link div was missing `flex-wrap` (pre-existing); now fixed
  - `comparisonTable()` now has `overflow-x-auto` wrapper — all 12 /vs pages benefit from this
---

## 2026-04-14 - BEAM-226
- Added UTM attribution to public dashboard share links in `src/routes/public.ts`
- Created `shareUrlWithUtm = shareUrl + ?utm_source=beam-share&utm_medium=referral`
- Used UTM URL for both: `copyShareLink()` JS (clipboard), and `tweetText` server-side (tweet pre-fill)
- Untagged `/public/:siteId` still works unchanged
- Smoke test verifies: clipboard content contains utm_source=beam-share, utm_medium=referral, and siteId; tweet href contains UTM params
- Uses `context.grantPermissions(['clipboard-read','clipboard-write'])` to read clipboard in Playwright
- Files changed: `src/routes/public.ts`, `test/smoke/smoke.spec.ts`
- Local 2/2 pass; prod 429 (IP signup rate limit from cumulative session) — not a code issue
- **Learnings for future iterations:**
  - `context.grantPermissions(['clipboard-read','clipboard-write'])` needed to read clipboard in Playwright tests
  - `shareUrl` JS var in `<script>` block needed updating alongside server-side tweetText — easy to miss one
---

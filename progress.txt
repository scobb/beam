# Ralph Progress Log
Started: Sat Apr 18 09:11:52 AM PDT 2026
---

## 2026-04-18 - Self-Generation Cycle
- All 245 stories were passing (BEAM-219 blocked); entered self-generation mode
- Audited prod (beam-privacy.com) with Playwright — all pages load cleanly, no JS errors
- **Key finding:** cdn.tailwindcss.com used on every page — 62 instances across 10+ route files; external HTTP request + browser console warning on every page load
- **Key finding:** No activation drip for signups who never install snippet (biggest churn risk)
- **Key finding:** No upgrade nudge when free users approach 50K pageview limit
- **Key finding:** No viral loop / badge on public stats pages
- **Key finding:** No annual pricing option
- Generated 10 new stories (BEAM-247 through BEAM-256):
  - BEAM-247 (high): Replace CDN Tailwind with self-hosted CSS bundle
  - BEAM-248 (high): Activation drip email for signups without snippet
  - BEAM-249 (high): Free plan upgrade nudge at 80% pageview limit
  - BEAM-250 (medium): 'Analytics by Beam' badge on public stats pages
  - BEAM-251 (medium): Annual pricing at $50/yr
  - BEAM-252 (medium): /vs/umami, /vs/simple-analytics, /vs/posthog
  - BEAM-253 (medium): /for/ecommerce, /for/saas vertical landing pages
  - BEAM-254 (low): Blog — GDPR analytics mistakes
  - BEAM-255 (low): Blog — indie hacker's guide to privacy analytics
  - BEAM-256 (low): Pro dashboard 'add another site' prompt
- Files changed: prd.json, ralph-context.md, progress.txt
---

## 2026-04-18 - BEAM-247
- Replaced CDN Tailwind with self-hosted compiled CSS bundle
- Installed tailwindcss v4.2.2 + @tailwindcss/cli as dev dependencies
- Created src/input.css (`@import "tailwindcss"; @source "../src"`)
- Created scripts/build-css.mjs: compiles dist/tailwind.css → src/tailwindCss.ts TypeScript const
- Created src/routes/assets.ts: serves GET /assets/tailwind.css (text/css, long-lived cache)
- Replaced all 62 `<script src="https://cdn.tailwindcss.com">` tags with `<link rel="stylesheet" href="/assets/tailwind.css">` across 18 files
- Removed stale `<link rel="preconnect" href="https://cdn.tailwindcss.com">` from dashboard.ts
- Updated package.json: build/dev/deploy:raw now run build:css first
- Zero browser console warnings on all pages after deploy
- **Learnings for future iterations:**
  - Tailwind v4 uses `@import "tailwindcss"` not the old `@tailwind base/components/utilities` directives
  - Tailwind v4 CLI is a separate `@tailwindcss/cli` package — no binary at node_modules/.bin/tailwindcss
  - Compiled CSS (56KB) embedded as TypeScript const avoids Cloudflare Workers static asset routing complexity
  - `dist/` is gitignored — only commit src/tailwindCss.ts (the generated TS constant)
  - BEAM-225 local smoke tests fail when run in isolation (miniflare signup rate limiting), not a real regression
---

## 2026-04-18 - BEAM-248
- Added activation drip email for new signups who haven't installed the snippet within 24 hours
- Migration 0017: `activation_email_sent_at TEXT` column on users table
- `src/lib/activationDrip.ts`: pure functions for 20-28h eligibility window (unit tested)
- `runActivationDrip()` in scheduled.ts: queries users created 20-28h ago with no pageviews, sends email, sets sent_at to prevent duplicates
- Email includes HTML snippet, setup guide link, /for framework guides link
- Handles users with no sites (links to /dashboard/new-site)
- Runs alongside traffic-alerts on every daily cron via nexusTrace
- 5 unit tests, 4 smoke tests (all pass on prod)
- **Learnings for future iterations:**
  - page.evaluate() with relative URL fails if no page has been navigated to first (about:blank context has no origin)
  - Prod login page text is "Log In" (capital I), not "Log in"
  - Staging migration showed "No migrations to apply" — staging may have received schema changes from a prior partial deploy
---

## 2026-04-18 - BEAM-249
- What was implemented: Free plan upgrade nudge banners on the analytics page
  - Yellow warning banner when free user reaches ≥40K pageviews/month
  - Red urgent banner when free user reaches ≥50K pageviews/month (plan limit)
  - Pro users never see the banner (isProUser guard)
  - Banner placement: above importCoverageBannerHtml, below filter chips
  - Both banners include a link to /dashboard/billing
  - data-testid="upgrade-nudge-warn" and data-testid="upgrade-nudge-urgent" for testing
- Files changed: src/routes/dashboard.ts, test/smoke/smoke.spec.ts
- **Learnings for future iterations:**
  - PROD SIGNUP RATE LIMIT: 10/hour/IP — tests using signupAndGetSession must run locally, not against prod
  - Month-to-date query pattern: `SELECT COUNT(*) as count FROM pageviews WHERE site_id = ? AND timestamp >= ?` with monthStart = `new Date(Date.UTC(year, month, 1)).toISOString()`
  - user.plan === 'pro' is the correct check for plan type (string comparison)
---

## 2026-04-18 - BEAM-250
- What was implemented: 'Analytics by Beam' badge on public stats pages with toggle
  - Footer badge with "Analytics by Beam — Privacy-first, cookie-free" on all public stats pages by default
  - Added data-testid="beam-badge-footer" to footer for testability
  - New DB migration: `hide_beam_badge INTEGER DEFAULT 0` on sites table (0018)
  - Site settings page shows "Beam Badge: On/Off" toggle (only when public dashboard is enabled)
  - POST /dashboard/sites/:id/badge handler toggles the column
  - public.ts site query includes COALESCE(hide_beam_badge, 0) and conditionally renders footer
- Files changed: migrations/0018_add_hide_beam_badge.sql, src/routes/public.ts, src/routes/dashboard.ts, test/smoke/smoke.spec.ts
- **Learnings for future iterations:**
  - The Share button already contains beam-privacy.com in the Tweet URL — asserting `not.toContain('beam-privacy.com')` fails even when footer is hidden. Use data-testid on the footer element instead.
  - Migration applied locally with `--local` and to prod with `--remote` before deploying worker.
---

## 2026-04-18 - BEAM-251
- What was implemented: Annual pricing at $50/yr with monthly/annual billing toggle
  - Pricing page: JS toggle switches Pro card between "$5/mo" and "$50/yr + Save 17% callout"
  - Landing page: Added "or $50/yr — save 17%" under Pro price with link to /pricing
  - billing.ts checkout handler: reads `plan=annual` from body, uses STRIPE_ANNUAL_PRICE_ID if set
  - Billing page: free users see two upgrade buttons (Monthly $5/mo and Annual $50/yr Save 17%)
  - Billing page: Pro users on annual plan see "Annual plan · $50/yr" in current plan description
  - billingInterval detected by expanding Stripe subscription items (interval = 'year' or 'month')
  - STRIPE_ANNUAL_PRICE_ID added to types.ts Env interface
  - Test annual price created: price_1TNc2HRhEblTFzoxO4TpFpEM
  - **ACTION REQUIRED for Steve:** Create live Stripe annual price ($50/yr) via Stripe dashboard and set: `echo "price_XXXX" | wrangler secret put STRIPE_ANNUAL_PRICE_ID`
- Files changed: src/types.ts, src/routes/billing.ts, src/routes/pricing.ts, src/landing.ts, wrangler.toml, test/smoke/smoke.spec.ts
- **Learnings for future iterations:**
  - Stripe expand[] syntax: use `expand[]=data.items` in query string for subscription list
  - The live Stripe restricted key (rk_live_*) lacks rak_plan_read/write — cannot read or create prices via API from local environment. Full sk_live_ key is stored as Cloudflare Worker secret only.
---

## 2026-04-18 - BEAM-252
- What was implemented: Smoke tests for /vs/umami, /vs/simple-analytics, /vs/posthog
  - All three comparison pages were already fully implemented in src/routes/vs.ts
  - Pages include feature comparison tables, prose, related links, sitemap entries
  - Footer links and alternatives hub already included all three
  - Added smoke tests verifying each returns 200 with correct h1
- Files changed: test/smoke/smoke.spec.ts
- **Learnings for future iterations:**
  - Always check if a story's implementation already exists before writing code — vs.ts already had all three pages
---

## 2026-04-18 - BEAM-253
- What was implemented: /for/ecommerce and /for/saas vertical landing pages
  - Added `ecommerce` and `saas` entries to GUIDES in src/routes/for.ts
  - Added "Industry verticals" section to GUIDE_SECTIONS (keeps `as const`)
  - Each page: tagline, description, 3 installSteps, 4 whyPoints, others list
  - Ecommerce: covers Shopify theme.liquid, WooCommerce header.php, purchase event tracking
  - SaaS: covers marketing site snippet, signup event tracking, pricing page engagement
  - Both linked from /for hub via new "Industry verticals" section
  - Added /for/ecommerce and /for/saas to sitemap in index.ts (paths + meta)
- Files changed: src/routes/for.ts, src/index.ts, test/smoke/smoke.spec.ts
- **Learnings for future iterations:**
  - GUIDE_SECTIONS uses `as const` but adding new sections still works — just add to the array
  - GuideConfig requires: slug, name, icon, tagline, hubDescription, description, metaDescription, installSteps, verificationChecklist, whyPoints, others
---
